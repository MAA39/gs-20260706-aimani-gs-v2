# 夜間作業レポート 2026-07-08（PR #12 / feature/4-empty-box-deploy）

## やったこと（コミット一覧）

| コミット | 内容 |
|---|---|
| 1ca0b2b | レビューP0/P1一括対応 — sequence atomic化・throwゼロ・境界parse・認可をdomainへ・workflow防御（payload整合性/履歴snapshot/CAS保護/AIタイムアウト/失敗可視化） |
| d73c2ac | 1チャット1 in-flight run (TSU-004) + dispatch失敗の可視化 (ADV-008) |
| 0244a77 | vitest基盤 + 純粋ロジックテスト27本 (MIH-008)、CIで実テスト実行 |
| 888a60d | doc_index.jsonをgitignore |
| 5a9a5ab | Codex再レビュー(codex-11)対応 — REV-01/02/03/04 |
| ff74e2c | フロント品質改善 — v1検証済みUX資産の移植（IME保護Enter送信/カテゴリ開始ボタン/プライバシー文言） |

- typecheck: worker/web/全パッケージ green
- テスト: 27本 green（domain 11 / contracts 11 / shared 5）
- ローカルE2Eスモーク: 401認証ガード / 413 body limit / role注入→student固定、全て実機確認済み

Codex並列レビュー成果物: docs/codex/codex-07（仕分け）/08（v1資産）/09（テスト設計）/10（デッドコード監査）/11（再レビュー）の5本。

### codex-10（デッドコード/as cast監査）への裁定
- **A判定cast修正済み**: session.user.idを`parseMemberId`経由に（chat.ts/member.ts）
- **削除済み**: 未使用Brand型`UserId`/`QuestionCardId`
- **意図的に保持**: 未使用port群（`findByMember`=チャット一覧予定 / `markRepairing`+`listEventsAfter`=schemaの`repairing`状態・`ai_run_events`テーブルと対応する計画済みライフサイクル / `findByRole`・`findBySkills`=メンバーマッチング予定）。schemaが既にこれらを前提にしているため削除はしない
- **保留**: api-clientの`res.json() as T`（自APIレスポンスのparse。契約テスト導入とセットで対応すべき）、DB row→domainのB判定cast（enum検証の追加はMIH-004のUnion化と同時にやるのが効率的）

## 裁定（朝に確認してほしいこと）

1. **ChatNotOwned→404**（403ではなく）: chatId存在確認oracle（ADV-003）を塞ぐため、他人のchatは「存在しない」扱いに統一した。GitHub方式。403が良ければ`domainErrorToHttp`の1行変更。
2. **TSU-004は裁定A採用**: AI応答生成中の追加送信は409 `AI_RUN_IN_FLIGHT`。UIも送信ボタンを「AI応答待ち」表示で無効化。check-then-insertの微小race窓は残る（悪影響は応答混線でなくrun並走のみ、履歴snapshotで混線自体は防止済み）。
3. **TSU-005は裁定B採用**: AI失敗・timeout・dispatch失敗は全てsystemメッセージ「AI応答の生成に失敗しました…」としてchatに追記され、ユーザーに見える。
4. **TSU-002は裁定A採用**: `CreateMemberRequest`から`role`フィールドを契約ごと削除。サーバー側student固定。昇格フローは未設計（仕様化待ち）。
5. **ID形式**: UUID固定ではなく「URLセーフ英数・最大128字」で境界parse（better-authのuser idがUUIDでないため）。
6. **AIタイムアウト60秒**: FlueにキャンセルAPIが無いためprompt自体は中断不可。raceで敗北した応答は破棄される（コスト面のリークは残存、下記保留参照）。

## 触る手順（2行）

    pnpm --filter @gs-v2/worker dev   # API: flue dev (port 8787) ※素のwrangler devは古いdistを配信するので禁止
    pnpm --filter @gs-v2/web dev      # フロント (port 5173)

## 保留と理由

| 項目 | 理由 |
|---|---|
| TSU-007 Idempotency-Key | ヘッダ設計+既存レスポンス再現の仕様判断が必要。D1のidempotency_key UNIQUE制約は準備済み |
| ADV-007 message+aiRun非トランザクション | Port跨ぎtransactionの設計判断（統合Port or D1 batch専用メソッド）が必要。現状は失敗時にhuman messageだけ残る |
| ADV-010 composite FK / MIH-004 nullable Union化 | migrationのテーブル再構築が必要。**本番migration適用前の今が唯一やりやすいタイミング**なので朝に判断推奨 |
| REV-03残り（profile系フィールドの上限・URL形式・未知キー拒否） | parse.tsの強化。32KB body limitで最悪ケースは抑止済み |
| v1資産の残り（進捗文言/QuestionSheet/整理フェーズ/サイドバー） | chats一覧API・ai_run_events polling API・SharedReportドメインが未実装。docs/review/2026-07-08-v1-frontend-adoption.md参照 |
| AI promptのタイムアウト後コスト | Flue側にAbort APIが出たら対応 |

## 深夜第2部（まさかずさん指示: zenn調査 + 壁打ち + OAuth非依存実装）

### 追加実装（コミット e8f62ee〜）
- **チャット履歴一覧**: `GET /api/chats` + 空状態の「前回の続きから」UI + startChatのtitle自動設定
- **ai_run進捗polling**: `GET /api/ai-runs/:id` + UI進捗文言（接続中→材料整理中→形式調整中、v1実績コピー）+ failed即時エラー表示
- **ローカルdev接続修正**: Service Binding先のflue dev viteがHost:apiを拒否 → proxyをhttps://localhostに変更。dev registryでweb(5173)⇔worker(8787)が接続確認済み
- **codex-15の正しい反論を即修正**: 失敗systemメッセージをAI入力履歴から除外（信頼境界の混線解消）

### 罠の答え合わせ（codex-12/13: zenn-content flue-radio-explained全章 × gs-v2監査）
- **今夜のwrangler dev罠は本に未記載**（全文検索0件）→ 記事ネタになる新罠や。本の正規手順は一貫して`flue dev`
- **既に踏んでる罠3件**: ①runtime beta.2 vs cli beta.1の版ズレ ②`agents: ^0.16.2`の`^`固定なし ③素wrangler手順の取り違え（今夜実証）→ 朝: 版固定の判断推奨
- **これから踏む罠**: DO migration `v1`タグの編集禁止（class追加は新tag）、beta3破壊的変更（defineAgent/defineWorkflow + input契約への移行）

### 壁打ち結論（codex-14ロードマップ × codex-15建設的反対者）
- **一致した次の3手**: ①ai_run進捗polling（→今夜実装済み）②推薦stage最小実装+右パネルstub（Product Boundaryの本丸「人につなげる」）③message+aiRun原子性
- **体験の欠落トップ3（codex-15）**: 人につながる瞬間がない / 相談を外に出す承認動線がない / 会話→推薦材料への変換がない。**UIポリッシュより推薦が先**
- **裁定への反論と対応**: systemメッセージのAI混入→即修正済み。in-flight 409のcheck-then-insert race→DB invariant（部分UNIQUE index等）は朝判断。AI 60s「タイムアウト」→実態は表示上の待機上限（キャンセル不能）、命名と実コストは今後Flueのcancel API待ち
- **codex-15指摘のガードレール違反**: ADV-003等のWhyコメントがコード内に残存（AGENTS L0-5違反）→ 朝にADR化 or 削除の判断

## ★人間にしかできないこと

- `wrangler secret put` : BETTER_AUTH_SECRET / GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET / INTERNAL_ROUTE_SECRET
- GitHub OAuth App作成（Settings → Developer Settings → OAuth Apps）
- D1本番migration適用: `wrangler d1 migrations apply aimani-gs-v2-db --remote`（↑ADV-010のmigration判断の後推奨）
- PR #12 のマージ判断
